# ─── Load shared login environment ─────────────────────────────
# .zprofile contains PATH setup for brew, pyenv, go, ~/.local/bin
# The syntax is bash-compatible so we source it directly.
[[ -f ~/.zprofile ]] && source ~/.zprofile
